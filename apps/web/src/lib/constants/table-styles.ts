/** Sticky first column — use on `<th>` in wide scroll tables. */
export const stickyFirstThClass =
  "sticky left-0 z-[2] border-r border-border bg-muted/50";

/** Sticky first column — use on `<td>`; parent `<tr>` needs `group`. */
export const stickyFirstTdClass =
  "sticky left-0 z-[1] border-r border-border bg-card group-hover:bg-muted/50";